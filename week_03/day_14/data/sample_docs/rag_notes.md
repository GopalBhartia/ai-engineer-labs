# RAG Notes

RAG means Retrieval-Augmented Generation. It is an architecture where relevant information is retrieved from external documents before generating an answer with an LLM.

A RAG system usually has ingestion, parsing, chunking, embedding, vector storage, retrieval, generation, and citations.

RAG is useful when the answer should come from private, recent, or domain-specific documents instead of only the model's training data.