# FastAPI Notes

FastAPI is a Python framework for building APIs. It uses type hints and Pydantic models to validate request and response data.

FastAPI also provides automatic API documentation through Swagger UI. This makes it easy to test endpoints from the browser.

In an AI application, FastAPI can be used to expose endpoints such as chat, search, summarize, classify, and retrieve documents.